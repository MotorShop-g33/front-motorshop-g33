import styled from "styled-components";

export const DivInfo = styled.div`
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: flex-start;
  gap: 8px;
  margin-bottom: 16px;

  h1 {
    margin: 0;
  }

  .post-at {
    color: var(--gray-3);
  }
`;
